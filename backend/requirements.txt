# ===== Smart Grid AI Assistant — Core backend dependencies =====
# Compatible with Python 3.11, 3.12, AND 3.13 (Render + local).
# This file installs ONLY what is needed for STEP 2 (FastAPI + /health).
# Heavy ML deps live in requirements-ml.txt and are installed as we reach
# STEP 3+ (ingestion, embeddings, vector DB, agents, evaluation).

# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# --- Config & validation ---
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# --- Logging ---
python-json-logger==2.0.7

# --- HTTP + utility ---
httpx==0.27.2
requests==2.32.3
tenacity==9.0.0

# --- Data ingestion (STEP 3) — pandas/numpy/scipy with Py3.13 wheels ---
numpy==2.1.3
pandas==2.2.3
python-dateutil==2.9.0.post0

# --- Auth (multi-tenancy + JWT) ---
PyJWT==2.9.0

# --- PDF export (STEP 13) ---
reportlab==4.2.5

# --- WebSockets (STEP 13) ---
websockets==13.1

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0

# --- Retrieval engine (REQUIRED by core app: BM25 keyword + Chroma vector store) ---
# These were previously only in requirements-ml.txt, but app.rag.bm25_index and
# app.rag.vector_store import them at runtime. Without them BM25 reports
# "not_installed" and /incidents browse + semantic search degrade.
rank-bm25==0.2.2
chromadb>=0.5.20,<2.0
