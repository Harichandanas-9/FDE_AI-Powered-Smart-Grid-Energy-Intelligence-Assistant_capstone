# Backend API base URL.
# Empty = use Vite proxy (/api -> http://localhost:8000). Recommended for dev.
# Set to a full URL (e.g. https://smart-grid-backend.onrender.com) for production.
VITE_API_URL=
